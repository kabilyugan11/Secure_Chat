# Environment Variables for SecureChat
# Copy this file to .env.local and fill in your values

# NextAuth Configuration
# Generate a secret with: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-change-this-in-production
NEXTAUTH_URL=http://localhost:3000

# Pusher Configuration (for real-time messaging)
# Create a free account at https://pusher.com
PUSHER_APP_ID=your-pusher-app-id
PUSHER_SECRET=your-pusher-secret
NEXT_PUBLIC_PUSHER_KEY=your-pusher-key
NEXT_PUBLIC_PUSHER_CLUSTER=us2

# Note: For demo purposes, the app will work without Pusher
# Real-time features will be disabled, but encryption still works
